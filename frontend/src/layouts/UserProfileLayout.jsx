import { Outlet } from "react-router-dom"

const UserProfileLayout = () => {
    return (
        <>
            <div>

            </div>
            <Outlet />
        </>
    )
}

export default UserProfileLayout